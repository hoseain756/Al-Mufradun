class PageInfo {
  const PageInfo({
    required this.pageNumber,
    required this.startSura,
    required this.startAya,
  });

  final int pageNumber;
  final int startSura;
  final int startAya;
}
